

<table>
  <thead>
<!--     <tr>
      <th>Image</th>
      <th>Description</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr> -->
  </thead>

<div class="filter_options_container">

  <form class="filter_form">
    <%= select("occasion", "name", Occasion.all.collect(&:name)) %>
    <%= select("ethnicity", "name", Ethnicity.all.collect(&:name)) %>
    <input type="submit" value="Filter" />
  </form>
<br>
<%= link_to 'Clear Filters', root_path %>
</div>



  <tbody>
    <div id="images" class="transitions-enabled">
      <% @images.each do |image| %>
        <div class="box panel panel-default">
          <%= link_to image_tag(image.image.url(:medium)), image %>
        </div>
      <% end %>
    </div>
  </tbody>
</table>

<br>

<%= will_paginate @images, page_links: false %>

